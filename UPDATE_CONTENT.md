#### 更新内容
- **新增图像检测功能**：基于OpenCV模板匹配的图像检测模块，当前功能暂时与文字识别相同

#### 优化内容
- **颜色识别模块优化**：
  1. 启动后会先等待间隔时间再执行首次识别，而不是启动时就直接进行颜色识别
  2. 优化了大范围识别较小颜色时的识别逻辑，现在只要选择区域存在该颜色就会判断成功

#### 修复内容
- **双显示器坐标偏移修复**：修复了V2.1.X版本引入的双显示器环境下截图区域坐标计算错误的问题
