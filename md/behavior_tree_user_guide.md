最新版行为树工具下载链接：<https://pan.quark.cn/s/9ff74d0d3b05?pwd=F7Kw> 提取码：F7Kw

## 更新日志

### V3.0.4

**更新内容**

- **实现画布中节点多选能力**：现在可以通过框选或Ctrl+点击操作实现对画布内节点的多选操作，并且支持多选后的复制、删除、移动操作，将画布拖动修改至右键拖动，防止与多选功能冲突

**优化内容**

- **报警节点参数优化**：现在配置报警节点时，默认导入全局变量中的音乐文件和音量，并删除了报警次数参数，只用重复次数来控制报警次数
- **代码优化**：进行了一些代码层面优化，优化内容参考仓库：/md/behavior\_tree\_code\_audit\_report.md

**修复内容**

-  **修复报警节点不触发报警的BUG**

### V3.0.3

**更新内容**

- **新增报警节点**：支持播放报警音效，可自定义音频文件、音量、播放次数、播放间隔等参数，支持同步/异步播放模式
- **新增顺序节点"失败后继续执行"参数**：开启后，即使中间节点执行失败，仍会继续执行后续所有节点，最终根据是否有失败判定结果
- **节点名称动态显示**：画布实时显示用户自定义的节点名称，修改后立即更新

**优化内容**

- **文本超框自动处理**：超长节点名称自动截断并添加省略号，确保界面整洁美观
- **优化DD鼠标、键盘输入**：重构了DD版本鼠标键盘底层输入的代码，尝试修复部分用户反馈一些特殊按键无法操作的问题
-  **节点复制能力优化**：现在节点复制后，会自动复制所有参数，而不是初始参数

**修复内容**

- **修复点击节点后立刻移动鼠标会导致节点偏移**的问题

***

### V3.0.2

**更新内容**

- **图像匹配节点**：新增截图功能，可直接从屏幕截取模板图像并自动保存到项目目录

**优化内容**

- **优化节点状态灯UI**：非运行状态下状态灯自动隐藏，避免遮挡节点名称，只在运行时显示状态信息
- **优化数字识别**：增加艺术字识别参数和置信度调整，**尝试**识别粗体彩色文字，和背景较为复杂的数字

**修复内容**

- **修复节点复制粘贴参数丢失**：修复了复制粘贴节点时，原节点的配置参数（名称、配置、启用状态等）没有被正确复制的问题，现在粘贴的节点会完整保留原节点的所有参数
- **修复数字识别无法保存点击位置的问题**：修复了成功识别到数字后，再使用鼠标点击，无法正确点击识别到数字的位置的问题

***

### V3.0.1

**更新内容**

- **新增鼠标滚轮节点**：支持垂直和水平滚动，可配置滚动距离和滚动次数
- **扩展鼠标点击节点**：支持多击功能（单击、双击、三击等），支持无限点击（-1）
- **扩展鼠标移动节点**：支持拖拽功能，可配置拖拽按钮、终点位置和拖拽时长

**优化内容**

- **优化画布缩放逻辑**：以鼠标位置为中心进行缩放，提升用户体验
- **优化重置视图功能**：自动定位到节点位置并居中显示，避免缩放后找不到节点

**修复内容**

无

***

## 目录

1. [快速入门](#1-快速入门)
2. [界面介绍](#2-界面介绍)
3. [节点操作](#3-节点操作)
4. [连接操作](#4-连接操作)
5. [属性配置](#5-属性配置)
6. [节点类型详解](#6-节点类型详解)
7. [运行与调试](#7-运行与调试)
8. [文件操作](#8-文件操作)
9. [快捷键速查](#9-快捷键速查)
10. [常见问题](#10-常见问题)

***

## 1. 快速入门

### 1.1 什么是行为树？

行为树是一种可视化的自动化流程设计工具。通过拖拽节点并连接它们，您可以创建复杂的自动化任务，例如：

- 自动检测屏幕上的文字或图像
- 自动点击按钮
- 自动执行重复性操作

### 1.2 创建第一个行为树

**步骤1：创建新文件**

- 点击工具栏的【新建】按钮
- 或按快捷键 `Ctrl + N`

**步骤2：添加节点**

- 从左侧节点面板点击需要的节点
- 节点会自动添加到画布中央

**步骤3：连接节点**

- 从父节点底部的圆点拖拽到子节点顶部的圆点
- 释放鼠标完成连接

**步骤4：配置属性**

- 点击选中节点
- 在右侧属性面板配置参数

**步骤5：保存运行**

- 点击【保存】按钮保存文件
- 点击【运行】按钮执行行为树

***

## 2. 界面介绍

### 2.1 整体布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│                              工具栏                                      │
│  [新建] [打开] [保存] │ [撤销] [重做] [清空] │       文件路径          [重置] │
├────────────┬───────────────────────────────────────────┬────────────────┤
│            │                                           │                │
│   节点面板  │                                           │      属性面板    │
│            │                                           │                │
│  ▼ 组合节点 │                                           │   节点属性      │
│   ◇ 顺序   │                                           │   ─────────    │
│   ◇ 选择   │              画布区域                      │   名称: ___     │
│   ◇ 并行   │                                           │   启用: [✓]    │
│            │                                          │                │
│  ▼ 条件节点 │                                           │   配置参数      │
│   ◇ OCR检测│                                           │   ─────────    │
│   ◇ 图像匹配│                                           │                │
│   ◇ 颜色检测│                                           │   装饰参数      │
│   ◇ 数字比较│                                           │   ─────────    │
│   ◇ 变量判断│                                           │                │
│            │                                           │                │
│  ▼ 动作节点 │                                           │                │
│   ◆ 按键   │                                           │                │
│   ◆ 点击   │                                           │                │
│   ◆ 移动   │                                           │                │
│   ◆ 滚轮   │                                           │                │
│   ◆ 延时   │                                           │                │
│   ◆ 设变量 │                                           │                │
│   ◆ 代码   │                                           │                │
│   ◆ 脚本   │                                           │                │
│            │                                           │                │
└────────────┴───────────────────────────────────────────┴────────────────┘
```

### 2.2 工具栏说明

| 按钮 | 功能       | 快捷键      |
| -- | -------- | -------- |
| 新建 | 创建新的行为树  | Ctrl + N |
| 打开 | 打开已有文件   | Ctrl + O |
| 保存 | 保存当前文件   | Ctrl + S |
| 撤销 | 撤销上一步操作  | Ctrl + Z |
| 重做 | 重做已撤销操作  | Ctrl + Y |
| 清空 | 清空画布所有内容 | -        |
| 运行 | 开始执行行为树  | 空格键      |
| 停止 | 停止执行     | Escape   |

### 2.3 节点面板

节点面板包含三大类节点：

**组合节点**（菱形◇图标）：

- 顺序节点：按顺序执行所有子节点
- 选择节点：任一子节点成功即成功
- 并行节点：同时执行所有子节点

**条件节点**（菱形◇图标）：

- OCR检测：检测屏幕文字
- 图像匹配：匹配屏幕图像
- 颜色检测：检测屏幕颜色
- 数字比较：识别并比较数字
- 变量判断：判断变量值

**动作节点**（方形◆图标）：

- 按键：模拟键盘按键
- 点击：模拟鼠标点击（支持多击）
- 移动：移动鼠标位置（支持拖拽）
- 滚轮：鼠标滚轮滚动
- 延时：等待指定时间
- 设变量：设置变量值
- 代码：执行代码文件
- 脚本：执行脚本文件

### 2.4 属性面板

属性面板显示选中节点的配置选项：

```
┌────────────────────┐
│     节点属性       │
│   ─────────────   │
│   名称: [____]    │
│   启用: [✓]       │
│                    │
│     配置参数       │
│   ─────────────   │
│   检测区域: [...]  │
│   关键词: [____]   │
│   ...             │
│                    │
│     装饰参数       │
│   ─────────────   │
│   结果取反: [ ]    │
│   失败重试: [0]    │
└────────────────────┘
```

***

## 3. 节点操作

### 3.1 添加节点

1. 在左侧节点面板找到需要的节点
2. 点击节点按钮
3. 节点自动添加到画布中央

### 3.2 选择节点

| 操作        | 效果     |
| --------- | ------ |
| 单击节点      | 选中单个节点 |
| Ctrl + 单击 | 多选节点   |
| 单击空白区域    | 取消选中   |
| 单击连线      | 选中连线   |

### 3.3 移动节点

1. 在节点非端口区域按住鼠标左键
2. 拖拽到目标位置
3. 释放鼠标完成移动

**提示**：移动节点时，连接线会自动更新位置

### 3.4 删除节点

| 方法   | 操作                          |
| ---- | --------------------------- |
| 快捷键  | 选中后按 `Delete` 或 `Backspace` |
| 右键菜单 | 右键点击 → 选择"删除"               |

### 3.5 复制粘贴

| 快捷键      | 功能          |
| -------- | ----------- |
| Ctrl + C | 复制选中节点      |
| Ctrl + V | 粘贴节点        |
| Ctrl + D | 快捷复制（复制并粘贴） |

**复制内容说明**：

复制节点时会完整复制以下内容：

- 节点名称
- 所有配置参数（如检测区域、关键词、阈值等）
- 节点启用状态
- 装饰参数（如重试次数、结果取反等）

**提示**：粘贴的节点会自动偏移50像素，避免完全重叠

***

## 4. 连接操作

### 4.1 节点端口说明

每个节点有两个端口：

- **输入端口**：节点顶部的圆点，用于接收父节点的连接
- **输出端口**：节点底部的圆点，用于连接子节点

```
        ○ 输入端口
    ┌─────────┐
    │  节点    │
    └─────────┘
        ○ 输出端口
```

### 4.2 创建连接

1. 将鼠标移到父节点的**输出端口**（底部圆点）
2. 光标变为十字形状时，按住左键拖拽
3. 拖拽到子节点的**输入端口**（顶部圆点）
4. 释放鼠标完成连接

```
创建连接示意：

    ┌─────────┐
    │ 父节点   │
    └────┬────┘
         ○ 输出端口
         │
         │  拖拽连线
         │
         ○ 输入端口
    ┌────┴────┐
    │ 子节点   │
    └─────────┘
```

### 4.3 删除连接

| 方法   | 操作              |
| ---- | --------------- |
| 快捷键  | 选中连线后按 `Delete` |
| 右键菜单 | 右键点击连线 → 选择"删除" |

### 4.4 连接规则

- 一个输出端口可以连接多个子节点
- 一个输入端口只能连接一个父节点
- 不能创建循环连接（子节点连接回父节点）

### 4.5 执行顺序

当组合节点有多个子节点时，连接线上会显示序号（1、2、3...）表示执行顺序。

**调整执行顺序**：

1. 删除需要调整的连接线
2. 按期望顺序重新连接

***

## 5. 属性配置

### 5.1 基本属性

所有节点都有的基本属性：

| 属性 | 说明               |
| -- | ---------------- |
| 名称 | 节点的显示名称，便于识别     |
| 启用 | 是否启用该节点（禁用后跳过执行） |

### 5.2 区域选择

条件节点通常需要设置检测区域：

**手动输入**：

- 在区域输入框输入坐标：`x1,y1,x2,y2`
- 例如：`100,100,300,200`

**使用选择工具**：

1. 点击区域输入框右侧的【选择】按钮
2. 在屏幕上拖拽选择区域
3. 释放鼠标后自动填入坐标

### 5.3 颜色选择

颜色检测节点需要选择目标颜色：

1. 点击颜色选择框右侧的【选择】按钮
2. 在屏幕上点击目标颜色
3. 颜色值自动填入

**颜色预览**：

- 左侧色块显示当前选择的颜色
- 右侧文本框显示RGB值

### 5.4 文件选择

图像匹配、代码执行等节点需要选择文件：

1. 点击文件输入框右侧的【浏览】按钮
2. 在文件对话框中选择文件
3. 文件路径自动填入

**提示**：浏览时默认打开当前行为树文件所在目录

### 5.5 装饰参数

装饰参数用于改变节点的执行行为：

**结果取反**：

- 勾选后，成功变为失败，失败变为成功
- 用于检测"不存在"的情况

**失败重试**：

- 设置节点失败后的重试次数
- 例如设置为3，则最多尝试4次（首次+3次重试）

***

## 6. 节点类型详解

### 6.1 组合节点

#### 顺序节点

**用途**：按顺序执行所有子节点，全部成功才成功。

**执行流程**：

```
          顺序
           │
   ┌───────┼────────┐
   ↓       ↓        ↓
子节点1 → 子节点2 → 子节点3 → ...
  ↓失败       ↓失败       ↓失败
返回失败    返回失败    返回失败
```

**参数**：

| 参数      | 说明                                |
| ------- | --------------------------------- |
| 子节点间隔   | 每个子节点执行完成后的等待时间（毫秒）               |
| 失败后继续执行 | 开启后，中间节点失败仍继续执行后续节点，最终根据是否有失败判定结果 |

**失败后继续执行说明**：

默认情况下，顺序节点采用"短路失败"策略：任一子节点失败立即终止，后续节点不执行。

开启"失败后继续执行"后：

- 子节点失败时不会立即终止
- 继续执行后续所有子节点
- 所有子节点执行完毕后，检查是否有失败的节点
- 如果存在失败的子节点，顺序节点返回失败
- 如果所有子节点都成功，顺序节点返回成功

```
示例：顺序节点 [A] → [B] → [C] → [D]，开启"失败后继续执行"

情况：节点 B 执行失败

执行结果：
├── [A] 执行成功 ✓
├── [B] 执行失败 ✗ ← 记录失败，继续执行
├── [C] 执行成功 ✓ ← 仍然执行
└── [D] 执行成功 ✓ ← 仍然执行

最终结果：返回失败（因为 B 失败了）
```

**使用场景**：

- 需要按固定顺序执行的任务
- 流程化的自动化操作
- 需要执行所有清理操作，即使中间步骤失败
- 批量操作，需要知道每个操作的结果

**示例**：登录流程

```
顺序节点
├── OCR检测：检测登录按钮
├── 鼠标点击：点击登录按钮
├── 延时：等待2秒
└── OCR检测：检测登录成功
```

#### 选择节点

**用途**：按顺序执行子节点，任一成功即成功。

**执行流程**：

```
子节点1 → 子节点2 → 子节点3 → ...
  ↓成功       ↓成功       ↓成功
返回成功    返回成功    返回成功
```

**参数**：

- 子节点间隔：每个子节点执行完成后的等待时间（毫秒）

**使用场景**：

- 多种方案尝试，任一成功即可
- 容错处理，提供备选方案

**示例**：多方式登录

```
选择节点
├── OCR检测：检测账号密码登录入口
├── OCR检测：检测扫码登录入口
└── OCR检测：检测第三方登录入口
```

#### 并行节点

**用途**：同时执行所有子节点。

**执行流程**：

```
       并行
         │
   ┌─────┼─────┐
   ↓     ↓     ↓
子节点1 子节点2 子节点3
   ↓     ↓     ↓
   └─────┼─────┘
         ↓
     汇总结果
```

**参数**：

- 成功策略：
  - 全部成功：所有子节点成功才成功
  - 任一成功：任一子节点成功即成功

**使用场景**：

- 同时监控多个条件
- 并行执行独立任务

### 6.2 条件节点

#### OCR检测节点

**用途**：检测屏幕指定区域的文字内容。

**参数**：

| 参数   | 说明                        |
| ---- | ------------------------- |
| 检测区域 | 屏幕上的检测范围                  |
| 关键词  | 要检测的文字（多个用逗号分隔）           |
| 语言   | OCR识别语言（eng/chi\_sim/jpn） |

**使用技巧**：

- 关键词可以是多个，用逗号分隔
- 检测成功后会自动保存位置，供点击节点使用

#### 图像匹配节点

**用途**：检测屏幕指定区域是否包含目标图像。

**参数**：

| 参数   | 说明                  |
| ---- | ------------------- |
| 检测区域 | 屏幕上的检测范围            |
| 模板路径 | 目标图像文件路径            |
| 匹配阈值 | 匹配度百分比（0\~100），默认80 |

**获取模板图像的方式**：

1. **截图功能**（推荐）：
   - 点击"模板路径"下方的【截图】按钮
   - 工具窗口会自动最小化
   - 在屏幕上拖拽选择要截取的区域
   - 释放鼠标后，截图自动保存到项目目录的 `/image/` 文件夹
   - 截图路径自动填入"模板路径"字段
2. **浏览选择**：
   - 点击【浏览】按钮选择已有的图像文件

**截图功能说明**：

- 截图会自动保存到当前行为树文件所在目录的 `/image/` 文件夹
- 如果该文件夹不存在，会自动创建
- 文件名格式：`template_YYYYMMDD_HHMMSS.png`（包含时间戳，避免重名）
- 截图完成后，工具窗口会自动恢复显示

**使用技巧**：

- 阈值越高，匹配越精确
- 建议阈值设置在80\~90之间
- 模板图像应尽量清晰、特征明显
- 使用截图功能可以直接从屏幕获取模板，方便快捷

#### 颜色检测节点

**用途**：检测屏幕指定区域是否包含目标颜色。

**参数**：

| 参数   | 说明                |
| ---- | ----------------- |
| 检测区域 | 屏幕上的检测范围          |
| 目标颜色 | 要检测的颜色（RGB值）      |
| 容差   | 颜色允许的偏差范围（0\~100） |

**使用技巧**：

- 容差越大，匹配越宽松
- 建议容差设置在10\~20之间

#### 数字比较节点

**用途**：识别屏幕上的数字并进行比较判断。

**参数**：

| 参数    | 说明                    |
| ----- | --------------------- |
| 检测区域  | 屏幕上的检测范围              |
| 预处理模式 | 图像预处理方式（普通文本/艺术字）     |
| 提取模式  | 数字提取方式                |
| 比较模式  | 比较运算符                 |
| 比较值   | 用于比较的阈值               |
| 置信度阈值 | 识别置信度最低要求（0-100，默认50） |
| 位置变量名 | 保存检测位置的变量名（用于点击最近检测点） |

**预处理模式**：

| 模式   | 适用场景                   |
| ---- | ---------------------- |
| 普通文本 | 普通文本数字（默认）             |
| 艺术字  | 粗体、彩色、艺术字数字，或背景较为复杂的情况 |

**提取模式**：

| 模式  | 说明        | 示例              |
| --- | --------- | --------------- |
| 无规则 | 直接采集第一个数字 | "HP: 100" → 100 |
| x/y | 识别分数中的分子  | "50/100" → 50   |
| 自定义 | 使用通配符自定义  | 见下方说明           |

**自定义提取模式**：

- 使用 `*` 表示要提取的数字
- 示例：
  - `"HP: *"` → 匹配 "HP: 100" 提取 100
  - `"(*/*)"` → 匹配 "(50/100)" 提取 50

**使用技巧**：

- 识别普通数字使用"普通文本"模式
- 识别游戏中的粗体、彩色、带特效的数字使用"艺术字"模式
- 如果识别不准确，可尝试降低置信度阈值
- 配合鼠标点击节点的"点击最近检测点"功能，可自动点击识别到的数字位置

### 6.3 动作节点

#### 鼠标点击节点

**用途**：模拟鼠标点击操作，支持单击、双击、多击和无限点击。

**参数**：

| 参数      | 说明                                      |
| ------- | --------------------------------------- |
| 按钮      | 鼠标按钮（左键/右键/中键）                          |
| 动作      | 点击动作类型                                  |
| 按住时长    | 按住鼠标的时间（毫秒）                             |
| 位置      | 点击位置（可选）                                |
| 点击最近检测点 | 勾选后使用最近检测到的位置                           |
| 位置变量名   | 检测位置的黑板变量名（默认last\_detection\_position） |
| 点击次数    | 点击次数（1-10次，-1为无限点击）                     |
| 点击间隔    | 多次点击之间的间隔时间（毫秒）                         |

**动作类型**：

| 类型    | 说明             |
| ----- | -------------- |
| press | 完整点击（按下→等待→抬起） |
| down  | 仅按下鼠标          |
| up    | 仅抬起鼠标          |

**点击次数说明**：

| 次数  | 说明           |
| --- | ------------ |
| 1   | 单击（默认）       |
| 2   | 双击           |
| 3   | 三击           |
| ... | ...          |
| 10  | 十击           |
| -1  | 无限点击（直到手动停止） |

**使用技巧**：

- 普通点击：动作=press，按住时长=0，点击次数=1
- 长按：动作=press，按住时长=500（毫秒），点击次数=1
- 双击：点击次数=2，点击间隔=100（毫秒）
- 三击：点击次数=3，点击间隔=100（毫秒）
- 无限点击：点击次数=-1，点击间隔=100（毫秒），持续点击直到按Escape键停止
- 拖拽：配合移动节点的拖拽功能

**无限点击使用场景**：

- 需要持续点击某个按钮直到特定条件满足
- 游戏中的连击操作
- 需要手动控制停止时机的点击操作

**示例**：

```
无限点击示例：
1. OCR检测：检测目标按钮
2. 鼠标点击：点击次数=-1，点击间隔=100，点击最近检测点=是
3. 条件判断：检测是否满足停止条件
4. 停止操作：按Escape键停止无限点击
```

#### 鼠标移动节点

**用途**：移动鼠标位置，支持普通移动和拖拽操作。

**参数**：

| 参数       | 说明                   |
| -------- | -------------------- |
| 位置       | 目标位置（拖拽时为起点）         |
| 移动到最近检测点 | 勾选后使用最近检测到的位置        |
| 黑板变量名    | 黑板变量名                |
| 相对移动     | 是否相对当前位置移动           |
| 移动类型     | 移动或拖拽                |
| 拖拽按钮     | 拖拽时按住的鼠标按钮（左键/右键/中键） |
| 拖拽终点     | 拖拽的目标位置              |
| 拖拽时长     | 拖拽过程时长（毫秒），0表示立即移动   |

**移动类型**：

| 类型 | 说明             |
| -- | -------------- |
| 移动 | 普通移动，直接移动到目标位置 |
| 拖拽 | 按住鼠标按钮并移动到目标位置 |

**使用技巧**：

- 普通移动：移动类型=移动，设置目标位置
- 相对移动：相对移动=是，位置=(100,0) 表示向右移动100像素
- 拖拽文件：移动类型=拖拽，位置为起点，拖拽终点为目标位置
- 平滑拖拽：拖拽时长=500-1000（毫秒），拖拽过程更平滑
- 使用黑板：勾选"移动到最近检测点"，自动使用最近检测到的位置

**拖拽功能说明**：

拖拽功能会自动执行以下步骤：

1. 移动到起点位置
2. 按住指定的鼠标按钮
3. 平滑移动到终点位置（如果设置了拖拽时长）
4. 释放鼠标按钮

**示例**：

```
拖拽操作示例：
1. OCR检测：检测文件图标
2. 鼠标移动：移动类型=拖拽，移动到最近检测点=是，拖拽终点=(500,300)，拖拽时长=500
```

#### 鼠标滚轮节点

**用途**：模拟鼠标滚轮滚动操作。

**参数**：

| 参数   | 说明           |
| ---- | ------------ |
| 滚动距离 | 每次滚动的距离（1-N） |
| 滚动次数 | 滚动次数（1-N）    |
| 滚动方向 | 向上/向下/向左/向右  |

**滚动方向说明**：

| 方向 | 说明     |
| -- | ------ |
| 向上 | 向上滚动页面 |
| 向下 | 向下滚动页面 |
| 向左 | 向左滚动内容 |
| 向右 | 向右滚动内容 |

**使用技巧**：

- 小距离滚动：滚动距离=3-5，滚动次数=5-10，适合微调页面位置
- 中等滚动：滚动距离=10-20，滚动次数=2-5，适合滚动一屏内容
- 大距离滚动：滚动距离=30-50，滚动次数=1-3，适合快速滚动多屏内容
- 总滚动距离 = 滚动距离 × 滚动次数

**使用场景**：

- 滚动页面查看内容
- 缩放页面（配合Ctrl键）
- 滚动列表选择项目

**示例**：

```
向下滚动一屏：滚动距离=20，滚动次数=1，滚动方向=向下
快速滚动多屏：滚动距离=50，滚动次数=3，滚动方向=向下
精细调整：滚动距离=5，滚动次数=10，滚动方向=向下
```

#### 按键节点

**用途**：模拟键盘按键操作。

**参数**：

| 参数   | 说明          |
| ---- | ----------- |
| 按键   | 要按下的键       |
| 动作   | 按键动作类型      |
| 按住时长 | 按住按键的时间（毫秒） |

**常用按键**：

- 字母：a\~z
- 数字：0\~9
- 功能键：f1\~f12
- 控制键：ctrl, alt, shift, win
- 方向键：up, down, left, right
- 其他：enter, space, tab, escape

#### 延时节点

**用途**：等待指定时间。

**参数**：

| 参数   | 说明       |
| ---- | -------- |
| 延时时长 | 等待时间（毫秒） |

**使用场景**：

- 等待界面加载
- 等待动画完成
- 控制执行节奏

#### 代码执行节点

**用途**：执行外部代码文件。

**参数**：

| 参数   | 说明                             |
| ---- | ------------------------------ |
| 代码路径 | 代码文件路径                         |
| 代码类型 | 代码类型（自动/Python/批处理/PowerShell） |

**支持的代码类型**：

- Python脚本（.py）
- 批处理文件（.bat, .cmd）
- PowerShell脚本（.ps1）

#### 脚本执行节点

**用途**：执行Txt脚本文件，支持循环执行。

**参数**：

| 参数   | 说明         |
| ---- | ---------- |
| 脚本路径 | Txt脚本文件路径  |
| 循环执行 | 是否循环执行脚本内容 |

**脚本文件格式**：

脚本文件为普通文本文件，每行一个操作指令：

```
# 注释行
click 100,200
delay 1000
move 300,400
```

**支持的指令**：

| 指令        | 说明      | 示例            |
| --------- | ------- | ------------- |
| click x,y | 点击指定位置  | click 100,200 |
| move x,y  | 移动到指定位置 | move 300,400  |
| delay ms  | 延时（毫秒）  | delay 1000    |
| key 按键    | 按键操作    | key enter     |
| scroll 次数 | 滚轮滚动    | scroll -3     |

**使用场景**：

- 执行预定义的鼠标键盘操作序列
- 批量执行重复性操作
- 配合循环执行实现持续操作

**示例**：

```
# 自动点击脚本
click 100,200
delay 500
click 150,250
delay 500
move 200,300
```

#### 设变量节点

**用途**：设置、修改或删除黑板变量。

**参数**：

| 参数  | 说明                               |
| --- | -------------------------------- |
| 变量名 | 要操作的变量名                          |
| 操作  | 操作类型（set/increment/delete/clear） |
| 值   | 设置的值（set和increment时使用）           |

**操作类型**：

| 操作        | 说明     | 示例             |
| --------- | ------ | -------------- |
| set       | 设置变量值  | 变量名=count, 值=0 |
| increment | 递增变量值  | 变量名=count, 值=1 |
| delete    | 删除指定变量 | 变量名=count      |
| clear     | 清空所有变量 | 无需指定变量名和值      |

**使用场景**：

- 记录操作次数
- 存储检测结果
- 控制流程分支
- 传递数据给其他节点

**使用技巧**：

- 配合变量判断节点实现条件分支
- 使用increment实现计数器
- 使用set存储检测结果供后续使用

**示例**：

```
计数器示例：
1. 设变量：操作=set, 变量名=count, 值=0
2. OCR检测：检测目标
3. 设变量：操作=increment, 变量名=count, 值=1
4. 变量判断：判断count>=10
```

#### 报警节点 ⭐ 新增

**用途**：播放报警音效，用于提示、警告或通知。

**参数**：

| 参数     | 说明                     |
| ------ | ---------------------- |
| 音频文件   | 音频文件路径（留空使用全局默认报警音）    |
| 音量     | 播放音量 0-100（留空使用全局音量设置） |
| 播放次数   | 重复播放次数（默认1次）           |
| 播放间隔   | 多次播放时的间隔时间（毫秒）         |
| 等待播放完成 | 是否等待音频播放完成再继续执行        |

**音频文件支持**：

- MP3、WAV、OGG、FLAC 等常见格式
- 留空时自动使用工具设置中的全局报警音
- 如果全局设置也无效，使用项目自带的默认报警音

**播放模式**：

| 模式     | 说明              | 使用场景           |
| ------ | --------------- | -------------- |
| 等待播放完成 | 节点等待音频播放完成才继续执行 | 需要确保用户听到完整提示音  |
| 异步播放   | 节点立即返回，音频在后台播放  | 不需要等待，继续执行其他操作 |

**使用场景**：

- 检测到异常情况时播放报警音
- 任务完成时播放提示音
- 需要引起用户注意的场景
- 自定义成功/失败音效

**使用技巧**：

- 简单提示：使用默认设置即可
- 自定义音效：选择自己的音频文件
- 多次提醒：设置播放次数>1，配合播放间隔
- 后台提示：取消勾选"等待播放完成"，不阻塞流程

**示例**：

```
示例1：简单报警
OCR检测"错误" → 报警节点（默认设置）→ 点击"确定"按钮

示例2：自定义报警
图像检测特定图标 → 报警节点（自定义音频，音量80%，播放3次）→ 执行脚本

示例3：异步报警
OCR检测"完成" → 报警节点（异步播放）→ 发送按键"Ctrl+S"保存

示例4：多次提醒
检测到异常 → 报警节点（播放5次，间隔1000ms）→ 等待用户处理
```

***

## 7. 运行与调试

### 7.1 运行行为树

**启动运行**：

- 点击工具栏【运行】按钮（绿色）
- 或按快捷键 `空格键`

**运行状态显示**：

节点状态灯会在运行时自动显示，非运行时自动隐藏，避免遮挡节点名称。

| 状态  | 颜色   | 说明          |
| --- | ---- | ----------- |
| 空闲  | 默认   | 未执行（状态灯隐藏）  |
| 运行中 | 橙色闪烁 | 正在执行（状态灯显示） |
| 成功  | 绿色   | 执行成功（状态灯显示） |
| 失败  | 红色   | 执行失败（状态灯显示） |
| 已中止 | 灰色   | 被停止（状态灯显示）  |

**状态灯显示规则**：

- **非运行状态**：所有节点的状态灯自动隐藏，节点名称完整显示
- **运行状态**：状态灯自动显示，实时反映节点执行状态
- **停止运行**：所有节点状态重置，状态灯自动隐藏

### 7.2 停止执行

**停止方法**：

- 点击工具栏【停止】按钮（红色）
- 或按快捷键 `Escape`

### 7.3 查看日志

运行日志会显示在主界面的日志区域，包含：

- 节点执行状态
- 检测结果
- 错误信息

**日志级别**：

- `[INFO]`：普通信息
- `[BT]`：行为树相关
- `[ERROR]`：错误信息

### 7.4 调试技巧

1. **逐步测试**：先测试单个节点，再组合测试
2. **查看日志**：关注日志中的错误信息
3. **检查区域**：确保检测区域正确
4. **调整阈值**：图像匹配失败时尝试降低阈值
5. **使用延时**：在操作之间添加适当延时

***

## 8. 文件操作

### 8.1 新建文件

1. 点击【新建】按钮或按 `Ctrl + N`
2. 如果当前有未保存的更改，会提示保存
3. 创建新的空白行为树

### 8.2 打开文件

1. 点击【打开】按钮或按 `Ctrl + O`
2. 在文件对话框中选择 .json 文件
3. 打开后自动加载行为树

### 8.3 保存文件

**保存**：

- 点击【保存】按钮或按 `Ctrl + S`
- 首次保存会弹出文件对话框

**另存为**：

- 按 `Ctrl + Shift + S`
- 选择新的保存位置

### 8.4 自动保存

- 行为树会自动保存到配置文件
- 下次打开工具时自动加载上次的行为树

***

## 9. 快捷键速查

### 9.1 文件操作

| 快捷键              | 功能  |
| ---------------- | --- |
| Ctrl + N         | 新建  |
| Ctrl + O         | 打开  |
| Ctrl + S         | 保存  |
| Ctrl + Shift + S | 另存为 |

### 9.2 编辑操作

| 快捷键      | 功能   |
| -------- | ---- |
| Ctrl + Z | 撤销   |
| Ctrl + Y | 重做   |
| Ctrl + C | 复制   |
| Ctrl + V | 粘贴   |
| Ctrl + D | 快捷复制 |
| Delete   | 删除选中 |

### 9.3 运行控制

| 快捷键    | 功能      |
| ------ | ------- |
| 空格键    | 运行/停止切换 |
| Escape | 停止执行    |

***

## 10. 常见问题

### Q1: 如何实现循环执行？

**A**: 设置组合节点的"重复次数"为 -1 即可实现无限循环。

### Q2: 点击位置不正确怎么办？

**A**:

1. 检查检测区域是否正确
2. 确保勾选了"点击最近检测点"
3. 检测节点会保存绝对坐标，点击节点会使用该坐标

### Q3: OCR识别不准确怎么办？

**A**:

1. 确保检测区域只包含需要识别的文字
2. 尝试更换OCR语言设置
3. 系统会自动对图像进行预处理（灰度化、对比度增强、锐化、二值化）

### Q4: 图像匹配失败怎么办？

**A**:

1. 降低匹配阈值（如从90降到80）
2. 确保模板图像清晰
3. 检查检测区域是否正确

### Q5: 如何使用"点击最近检测点"？

**A**:

1. 在条件节点（OCR/图像/颜色）中检测目标
2. 检测成功后位置会自动保存
3. 在点击节点中勾选"点击最近检测点"
4. 点击时会使用最近一次检测到的位置

### Q6: 修改属性后运行不生效怎么办？

**A**:

- 点击画布其他位置或切换选中节点，属性会自动保存
- 或手动按 `Ctrl + S` 保存

### Q7: 如何实现长按操作？

**A**:
在点击节点中设置：

- 动作：press
- 按住时长：500（毫秒）

### Q8: 如何实现拖拽操作？

**A**:
使用鼠标移动节点的拖拽功能：

1. 设置移动类型=拖拽
2. 设置位置为拖拽起点（或勾选"移动到最近检测点"）
3. 设置拖拽终点为目标位置
4. 设置拖拽时长（建议500-1000毫秒）

示例：

```
拖拽文件：
1. OCR检测：检测文件图标
2. 鼠标移动：移动类型=拖拽，移动到最近检测点=是，拖拽终点=(500,300)，拖拽时长=500
```

### Q9: 如何实现无限点击？

**A**:
在点击节点中设置：

- 点击次数：-1
- 点击间隔：100（毫秒）
- 节点会持续点击，直到按Escape键停止

使用场景：

- 需要持续点击直到满足条件
- 游戏中的连击操作
- 手动控制停止时机

### Q10: 节点执行顺序如何确定？

**A**:

- 连接线上会显示序号（1、2、3...）
- 按序号顺序执行
- 如需调整，删除连接线后重新按期望顺序连接

### Q10: 节点执行顺序如何确定？

**A**:

- 连接线上会显示序号（1、2、3...）
- 按序号顺序执行
- 如需调整，删除连接线后重新按期望顺序连接

### Q11: 如何提取特定格式的数字？

**A**:
使用数字比较节点的"自定义提取模式"：

- 要提取 "HP: 100/MAX" 中的 100
- 设置提取模式为"自定义"
- 设置自定义模式为 `HP: */MAX`
- 使用 `*` 表示要提取的数字部分

***

## 文档信息

- **版本**: 1.1
- **创建日期**: 2026-04-01
- **更新日期**: 2026-04-04
- **适用项目版本**: AutoDoor v3.0.3

